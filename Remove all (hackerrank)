#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int s;
    scanf("%d",&s);
    int arr[s];
    for(int i=0;i<s;i++){
        scanf("%d",&arr[i]);
    }
    int del;
    int list[s];
    int a=0;
    scanf("%d",&del);
    for(int i=0;i<s;i++){
        if(arr[i]!=del){
            list[a]=arr[i];
            a++;
        }
    }
    for(int i=0;i<a;i++){
        printf("%d ",list[i]);
    }
    return 0;
}
