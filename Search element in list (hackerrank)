#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int s;
    scanf("%d",&s);
    int arr[s];
    for(int i=0;i<s;i++){
        scanf("%d",&arr[i]);
    }
    int search;
    int flag=0;
    scanf("%d",&search);
    for(int i=0;i<s;i++){
        if(arr[i]==search){
            printf("Found");
            flag=1;
            break;
        }
    }
    if(flag==0){
        printf("Not Found");
    }
    return 0;
}
